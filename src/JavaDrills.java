import java.lang.StringBuilder.*;
//WORK IN PROGRESS
public class JavaDrills {
    public static String flipOuterCase(String word) {
       // String newString = '';
        for(int i = 0; i < word.length(); i++) {
            if(Character.isLowerCase(word.charAt(i))) {
                //newString.setCharAt() += word.charAt(i).
            }
        }
    }
}
